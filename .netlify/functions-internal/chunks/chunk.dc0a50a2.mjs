// Static
						const frontmatter = {"id":1,"desc":"Strategy & Engagements","name":"John Vermazen","imgSource":"/assets/img/about/team/1.webp","linkT":"#","linkLI":"#","info":" Strange to speak of peak efficiency and innovation, then hire talent in the same fashion as manual gang-labor through history, not at all following in the well-ordered footsteps of the Ford production line. Even worse that specifications would not be, well, precisely specified. A general attribute in the L to XXL programs he worked in as a tech consultant for institutional financial and information services in New York City was a persistent complete lack of mutual faith that either the client firm or the consulting were engaged in a common purpose with clear goals. "};
						const file = "C:/work-projects/pireactor/main-site/client/public/assets/leads/en/JohnVermazenSt.md";
						const url = undefined;
						function rawContent() {
							return "";
						}
						async function compiledContent() {
							return load().then((m) => m.compiledContent());
						}
						function $$loadMetadata() {
							return load().then((m) => m.$$metadata);
						}
						
						// Deferred
						async function load() {
							return (await import('./chunk.5a1f9d57.mjs'));
						}
						function Content(...args) {
							return load().then((m) => m.default(...args));
						}
						Content.isAstroComponentFactory = true;
						function getHeaders() {
							return load().then((m) => m.metadata.headers);
						}

export { $$loadMetadata, Content, compiledContent, load as default, file, frontmatter, getHeaders, rawContent, url };
